[project]
name = "how"
version = "0.1.0"
description = "how.domain.com build script"
requires-python = ">=3.11"
dependencies = [
    "python-frontmatter>=1.1",
    "mistune>=3.0",
    "jinja2>=3.1",
]
